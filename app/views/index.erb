<p><%= @message %></p>
<p>Session: <%= @home_info %></p>

<% if @articles.any? %>
  <ul>
    <% @articles.each do |article| %>
      <li><a href="/articles/<%= article.id %>"><%= article.title %></a></li>
    <% end %>
  </ul>
  <% else %>
    <p>No articles have been created yet!</p>
<% end %>
